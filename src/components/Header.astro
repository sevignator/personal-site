---
import Navigation from '@components/Navigation.astro';
import SiteLogo from '@components/SiteLogo.astro';
// TODO: Fix flash of unthemed content (FOUC) when the dark mode loads
// import ThemeToggle from '@components/_ThemeToggle.astro';
---

<header class="navbar">
  <nav class="container">
    <div class="navbar-left">
      <a class="logo-link" href="/">
        <SiteLogo colorPrimary="var(--clr-accent)" width="60" />
      </a>
    </div>

    <div class="navbar-right">
      <!-- <ThemeToggle /> -->
      <Navigation />
    </div>
  </nav>
</header>

<!-- CSS -->

<style>
  .navbar {
    height: fit-content;

    & .container {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding-block: 30px;
    }
  }

  .navbar-right {
    display: flex;
    align-items: center;
    gap: 20px;
  }

  .logo-link {
    display: block;
    line-height: 0;
  }
</style>
